1. **[TERSE]** Freelancer dash: stacked bar & pie by month/category, segment labels, metrics card: gross, fee, tax, net. Click bar to update pie and metrics.

2. **[CONVERSATIONAL]** I manage a portfolio of projects and want to build a risk heatmap. Enter tasks with effort hours, team size, weeks to deadline, and upstream dependency count. A task's risk should reflect how much effort each person carries, how entangled it is in dependencies, and how little time is left. Tasks with more dependencies than people to handle them are more dangerous. Show each task's share of portfolio risk as a heatmap where color intensity reflects how much of the risk that task owns. Tasks carrying more than equal split of total risk get flagged. I want a summary panel for the top 3 risks with each factor's percentage of the total score. Recalculates upon input change.

3. **[TERSE]** Drone assembly heatmap. Color components by labor savings 0-100%. Click for baseline vs optimized delta. Show total savings. Table sorted by savings.

4. **[CONVERSATIONAL]** I want to build a Markdown-to-Mindmap Engine. Make a split-screen app where I have a plain text area on one side for typing out nested bullet points. Pre-fill the text area with a small example nested list (like a 'Root' node with a few nested children) so the mind map is visible immediately on load. As I type, I want the other side of the screen to automatically generate and display a mind map based on my text. The mind map should show the bullet points as circles with the text inside or right next to them, and draw lines to connect parent and child nodes. Please make sure the layout spreads the nodes out neatly so nothing overlaps. It's super important that the mind map updates live while I'm typing

5. **[FEATURE_LIST]** Build a Sprint Velocity Dashboard. Pre-fill a textarea with CSV data for sprints with columns: sprint, completed, committed. Include a Parse button. Clicking Parse re-renders the dashboard from the current textarea content. Render the dashboard automatically on load without user interaction.

Display a bar chart with one bar per sprint for points completed, ordered by sprint sequence. Overlay a 3-sprint rolling average trend line starting at sprint 3 with visible markers at each computed point. Colour each completed bar green if its value meets or exceeds the rolling average at that sprint position, or red if below. Sprints 1 and 2 have no rolling average and must display bars in neutral blue.

Provide three filter buttons labelled "All", "Above Target", "Below Target", using a fixed target threshold of 30. Above means completed strictly greater than 30. "All" is selected by default with a visually distinct active state. Selecting a filter shows only matching sprints in the chart and recalculates the summary panel from the filtered subset exclusively.

Display a summary panel showing: average completed rounded to one decimal place with trailing zero, best sprint name and score, worst sprint name and score, and count of sprints above the target. When multiple sprints tie for best or worst in the filtered view, select the one appearing latest in sprint sequence. Display a velocity trend badge comparing the rolling average at the last filtered sprint to the rolling average at the second-to-last filtered sprint: show "Improving" if the increase exceeds 5%, "Declining" if the decrease exceeds 5%, or "Stable" otherwise. If fewer than 2 filtered sprints have rolling average values, show "N/A".

6. **[CASUAL]** Hey, can you build me a therapy session tracker dashboard?

On the left, give me an input panel for session topic, duration in minutes, and mood rating from 1 to 10, with an Add button.

Preload 5 sample sessions: "CBT: min mood 8, Anxiety: min mood 6. Relationships: min mood 9. Grief: min mood 5.

Mindfulness: min mood 7. "Show three shaped chart panels: a hexagon panel with a donut chart of time split by topic, a trapezoid panel with a calm jar that fills proportionally to total minutes logged with 300 minutes as a full jar, and a diamond panel with a scatter plot where each point is a heart emoji showing only sessions rated 7 or above plotted by duration on the x-axis and session index on the y-axis.

All charts update live when entries are added. Truncate labels longer than 8 characters with "...". Dark background with a distinct bright accent border on each panel.

7. **[CASUAL]** I need a quick workout visualizer app to track my exercise durations. Put a bold "Workout Visualizer" title at the very top of the page. Below that, I want a single number input for "Duration (minutes)" and an "Add Workout" button right next to it. Under the button, place a text display that starts off saying exactly "Total Time: 0 mins". Beneath all of this input stuff, give me a chart area where the bars sit evenly at the bottom. Whenever I click the "Add Workout" button, a new vertical bar should pop up inside that chart area. To keep the screen clean, the chart must only ever show a maximum of 5 bars; if I add a 6th workout, remove the oldest bar from the visual chart so the newest one fits. Make sure the height of each bar directly reflects the minutes entered, such that a 30-minute bar is exactly twice as tall as a 15-minute bar. The strict styling rule is that alternate the bar colors so that the 1st, 3rd, and 5th workouts ever entered are blue, while the 2nd, 4th, and 6th are green. Crucially, this alternating color must be based on the absolute total number of workouts added over time, meaning the 6th workout must be green even though it will be sitting in the 5th visual slot on the screen. Clicking the add button must also add the new duration to the running total and update the "Total Time" text to show the grand total of all workouts. Even when an old bar drops off the screen, its duration must remain permanently included in this running math. Finally, throw in a "Clear Data" button that wipes all the bars off the chart, completely resets the running math to zero, and changes the total time text exactly back to "Total Time: 0 mins" when I click it.

8. **[FEATURE_LIST]** Display altitude, distance, speed, and barometer profiles for the user's trekking activity upon page loading. On initial load, populate all profiles with sample trekking data showing a complete route with both ascent and descent segments. Below 2000m is the Vegetation zone, 2001-3000m is the Treeline zone, and above 3000m is the Alpine zone. Speed in the Vegetation, Treeline, and Alpine zones are in the ranges 5.0-9.5 km/hr, 4.5-6.5 km/hr, and 3.75-5.25 km/hr. 40% of the total trekking time spent in the Vegetation zone, 15% in the Treeline zone, and the rest in the Alpine zone. The user selects a point in one of the profiles to highlight values in the other profiles. Place zoom and pan buttons in the top-right corner of each profile chart. The user can choose unit conversions between the imperial and metric system via a toggle control positioned above the charts. The user didn't trek in the Treeline zone during ascent, but trekked all the zones during descent. The sample data should reflect this asymmetric pattern.

9. **[FEATURE_LIST]** Include a chart upon page loading. The chart lists twenty different image features including but not limited to flowers, cooking utensils, and motor vehicles. The chart also lists a number between 1 and 3 for each feature in a separate column. Construct four images, each of the images must have five different features. None of the images share any of the features with other images. The number of times a feature appears within an image is listed in the column right next to the feature name. The user can edit the feature list by clicking a cell in the chart. Display each image with the correct number of features. The images should be displayed in a 2x2 grid layout. The images are equal in terms of size. Display a counter with total number of features appeared in each of the images.

10. **[FEATURE_LIST]** Build a UI that visualises how a MinHeap sorting algorithm works. Users will enter numbers one by one. The numbers entered so far are displayed as well as a canvas that should contain a graph visualisation of the MinHeap tree. Every time the user submits a new number, the canvas should animate how the graph is updated - step by step following the MinHeap sorting algorithm. There should be an information displayed on how many node swaps it took to update the tree after most recent number was added - that number should be updated simultaneously with every step of the sorting algorithm. There should also be the sorted list displayed.
